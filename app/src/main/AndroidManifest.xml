<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todolistapps">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoListApps">
        <activity
            android:name="com.example.todolistapps.UpdateToDoListActivity"
            android:exported="true" />
        <activity
            android:name="com.example.todolistapps.DeleteToDoListActivity"
            android:exported="true" />
        <activity
            android:name="com.example.todolistapps.AddToDoListActivity"
            android:exported="true" />
        <activity
            android:name="com.example.todolistapps.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>